import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row',
        borderColor: '#f80',
        borderWidth: 1,
    },
    action: {
        paddingHorizontal: 8
    },
    actionText: {
        color: '#000',
        fontSize: 16
    },
    count: {
        color: '#000',
        fontSize: 12,
        paddingHorizontal: 4
    }
})